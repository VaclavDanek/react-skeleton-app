export const basename = '' // root pathname
export const authExpire = false
export const devTools = false

export const timers = {
  authExpireTimeout: 2 * 3600 * 1000,
}
export const ignoredErrorEventMessages = [
  'ResizeObserver loop limit exceeded'
]
